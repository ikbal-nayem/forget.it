import { makeStyles } from "@material-ui/styles";

export default makeStyles({
  active: {
    backgroundColor: '#ffffff42 !important',
    backdropFilter: 'blur(15px)',
    borderRadius: '10px'
  }
});